# branch-prediction-simulator
A simple C++ simulator for branch prediction using Bimodal and GShare predictors. Reads branch trace files, predicts taken/not-taken outcomes using 2-bit saturating counters, tracks mispredictions, and reports prediction accuracy for educational purposes.
